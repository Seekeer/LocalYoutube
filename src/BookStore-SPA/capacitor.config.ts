import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seekeer.localTube',
  appName: 'LocalTube',
  webDir: 'dist/BookStore-SPA',
};

export default config;