export class VideoManagerBase{


    // addToPlaylist(){
    //     this.seriesService.addToPlaylist(this.playlistId, this.videoId);
    //   }
    
    //   getPlaylists() {    
    //       this.seriesService.getPlaylists().subscribe(playlist => {
    //       this.playlists = playlist.sort((a, b) => {
    //         return a.name >= b.name
    //           ? 1
    //           : -1
    //       });
    //     });
    //   }
}
